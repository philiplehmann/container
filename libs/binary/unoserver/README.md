# binary-unoserver

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test binary-unoserver` to execute the unit tests via [Node test](https://nodejs.org/api/test.html).
