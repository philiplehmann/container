# docker executors

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build node` to build the library.

## Running unit tests

Run `nx test node` to execute the unit tests via [Node test](https://nodejs.org/api/test.html).
