{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Biomejs executor",
  "description": "",
  "type": "object",
  "properties": {
    "fix": {
      "type": "boolean",
      "required": false,
      "description": "format files"
    },
    "changed": {
      "type": "boolean",
      "required": false,
      "description": "When set to true, only the files that have been changed compared to your defaultBranch configuration will be linted."
    },
    "verbose": {
      "type": "boolean",
      "required": false,
      "description": "Print additional diagnostics, and some diagnostics show more information."
    },
    "log-level": {
      "type": "string",
      "enum": [
        "none",
        "debug",
        "info",
        "warn",
        "error"
      ],
      "required": false,
      "description": "The level of logging. In order, from the most verbose to the least verbose: debug, info, warn, error."
    }
  },
  "required": []
}