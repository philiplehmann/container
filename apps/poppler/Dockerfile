# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build poppler`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t poppler`.
FROM node:24.11.1-trixie-slim

LABEL org.opencontainers.image.authors="philiplehmann@gmail.com"

ARG POPPLER_VERSION

RUN --mount=type=cache,target=/var/lib/apt/lists,sharing=locked \
    --mount=type=cache,target=/var/cache/apt,sharing=locked \
    rm -f /etc/apt/apt.conf.d/docker-clean && \
    apt-get update && \
    apt-get install --no-install-recommends --yes poppler-utils=${POPPLER_VERSION}

WORKDIR /app

ENV HOST=0.0.0.0
ENV PORT=3000

COPY dist/apps/poppler poppler

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix poppler --omit=dev -f install

USER 1000:1000

CMD [ "node", "poppler" ]
