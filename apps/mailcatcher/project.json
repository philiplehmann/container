{
  "name": "mailcatcher",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mailcatcher/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@riwi/biomejs:lint",
      "outputs": ["{options.outputFile}"]
    },
    "typecheck": {
      "executor": "@riwi/tsc:typecheck"
    },
    "docker-test": {
      "executor": "@riwi/bun:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "dependsOn": ["docker-build-test"],
      "options": {}
    },
    "docker-build-test": {
      "executor": "@riwi/docker:test",
      "options": {
        "tag": "philiplehmann/mailcatcher:test",
        "file": "apps/mailcatcher/Dockerfile"
      }
    },
    "docker-push": {
      "executor": "@riwi/docker:build",
      "options": {
        "platforms": ["amd64", "arm64"],
        "tags": ["philiplehmann/mailcatcher:auto"],
        "file": "apps/mailcatcher/Dockerfile",
        "versionSource": "env",
        "versionSourceEnv": "MAILCATCHER_VERSION"
      }
    },
    "docker-run": {
      "executor": "@riwi/docker:run",
      "options": {
        "image": "philiplehmann/mailcatcher:run",
        "file": "apps/mailcatcher/Dockerfile",
        "port": ["1025", "1080"]
      }
    }
  }
}
